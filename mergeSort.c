#include "sync.h"

void merge(){

}

void mergeSort(int arr[]){
    int n = sizeof(arr) / sizeof(arr[0]);
    if (n > 1){
        for (int i = 0; i < n/2; i++){
            
        }
        mergeSort()
    }
}